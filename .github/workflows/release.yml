name: release

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - ".github/workflows/release.yml"
      - "packaging/**"
      - "scripts/package_release.py"
      - "scripts/prepare_pix2tex.py"
      - "scripts/vendor_katex.py"
      - "pyproject.toml"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: Ubuntu
          - os: windows-latest
            platform: Windows
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install Ubuntu Qt runtime libraries
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libegl1 \
            libgl1 \
            libxkbcommon-x11-0 \
            libxcb-cursor0 \
            libxcb-icccm4 \
            libxcb-image0 \
            libxcb-keysyms1 \
            libxcb-render-util0

      - name: Install CPU PyTorch
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

      - name: Install release dependencies
        run: python -m pip install -e ".[ocr,dev,build]"

      - name: Vendor offline KaTeX assets
        run: python scripts/vendor_katex.py

      - name: Prepare pix2tex checkpoints
        run: python scripts/prepare_pix2tex.py

      - name: Run tests
        run: python -m pytest -q

      - name: Run flake8
        run: python -m flake8 src tests scripts --max-line-length=100

      - name: Build standalone app
        run: python -m PyInstaller --noconfirm --clean packaging/FormulaOCR.spec

      - name: Create release ZIP
        run: >-
          python scripts/package_release.py
          --platform "${{ matrix.platform }}"
          --version "${{ github.ref_name }}"

      - uses: actions/upload-artifact@v4
        with:
          name: FormulaOCR-${{ matrix.platform }}
          path: release/*.zip
          if-no-files-found: error
          compression-level: 0

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "${{ github.ref_name }}"
          release/*.zip
          --title "Formula OCR ${{ github.ref_name }}"
          --generate-notes
